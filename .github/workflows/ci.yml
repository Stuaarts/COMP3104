

CI
ame: CI
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
- name: Verify repo
        run: |
          echo "CI is running on commit $GITHUB_SHA"
          ls -la
